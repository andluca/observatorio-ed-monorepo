import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  experimental: {
    // reactCompiler: true, // Descomente apenas se tiver certeza que quer usar o compilador experimental
  },
};

export default nextConfig;